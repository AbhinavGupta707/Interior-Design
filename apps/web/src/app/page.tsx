export default function HomePage() {
  return (
    <main>
      <section aria-labelledby="title">
        <p>Complete Home Design System</p>
        <h1 id="title">Your home, understood before it is redesigned.</h1>
        <p>
          C0 establishes the secure web, API and native-capture foundations. Home evidence,
          reconstructed geometry, generated design media and implementation information remain
          explicitly versioned and source-aware.
        </p>
      </section>
    </main>
  );
}
