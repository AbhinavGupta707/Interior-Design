# Development runbooks

- `clean-bootstrap.md` — install, start, verify, troubleshoot, and safely stop every C0 surface.
- `integration-requests.md` — proposed root command aliases for the orchestrator; no root manifest is
  modified by C0-L4.

The executable dependency definition is `infrastructure/local/compose.yaml`. The provider-free
boundary contract is under `infrastructure/iac/`.
